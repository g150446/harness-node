# シングルタップ（`0x14`）イベント対応

> ファームウェア **0.0.93+**: single (`0x14`) が全モードで録音トグル（FW 所有）。
> double (`0x12`) は録音しない（Kindle/G2 / Handy control 専用）。
> ホストは TX `0x01`/`0x02` でセッションに追従し、`0x14` では RX 録音コマンドを送らない。

関連（実装済み）:

- ファーム仕様: [`nordic_main_guide.md`](nordic_main_guide.md) の「シングル／ダブルタップ」
- 実装: `nordic-main/src/main.c`（`EVT_SINGLE_TAP` / `process_tap_event`）

---

## 現状

| 層 | 状態 |
|----|------|
| ファーム `0.0.76+` | HW シングル／ダブルタップ検出、BLE 通知 |
| Mac クライアント | `0x14` 未対応（任意のデバッグ表示のみ後追い可） |
| **Android アプリ** | `0x14`をパースし診断ログへ記録 |

---

## プロトコル（Android が受けるもの）

### BLE 基本

| 項目 | 値 |
|------|-----|
| デバイス名 | `HarnessNode` |
| Service | `00000001-0000-1000-8000-00805f9b34fb` |
| TX（Notify） | `00000002-0000-1000-8000-00805f9b34fb` |
| RX（Write） | `00000003-0000-1000-8000-00805f9b34fb` |

イベントパケット（TX notify）:

```
[0x00][0x55][code][+ optional payload]
```

### タップ関連コード

| code | 名前 | payload | 意味 |
|------|------|---------|------|
| `0x12` | `double_tap` | なし | リストバンド表側→基板面の垂直ダブルタップ |
| **`0x14`** | **`single_tap`** | **なし** | 同上のシングルタップ（間隔窓を超えた単独衝撃） |
| `0x21` | `sleep_wake` | なし | ライトスリープ復帰（タップでも発生し得る） |
| `0x01` / `0x02` | recording start/stop | なし | 録音開始／停止（ジェスチャー or **single tap/click**） |

**注意:** `0x13` は別用途（IMU sample デバッグ）で使われている。`single_tap` は **`0x14` 固定**。

### ファーム側セマンティクス（Android / Handy 設計の前提）

| モード | `0x14` single | `0x12` double |
|--------|---------------|---------------|
| 通常 | primary 接続中に録音トグル + `0x14`。続けて `0x01`/`0x02` | 通知のみ（録音不変）。開始ジェスチャー候補を破棄 |
| 運転 | 同上（ジェスチャー無効のまま） | 通知のみ（録音不変） |

タイミング:

- 物理 1 打目の時点で HW が INT1 をassertする（`MD1_CFG` に single も割り当てているため）
- ファームは **`TAP_SRC` に触れずに 350 ms 待つ**。ここで読むとラッチが解除され、
  進行中のダブルタップ判定が壊れる（0.0.76〜0.0.86 の `0x12` 消失の真因）
- 待機後に `TAP_SRC` を1回だけ読む。`TAP_IA|DOUBLE|Z`（`0x51`/`0x59`）なら double、
  それ以外（実測 `0x00`）なら single
- したがって Android から見ると、**タップ1回につきイベントは必ず1つだけ**。
  double の直前に single が来ることはない
- 物理タップから BLE 通知までの遅延は約 **350〜400 ms**（single / double とも同じ）
- クールダウンは **700 ms**、single と double で別々（0.0.81〜）

ライトスリープ中のタップ: 先に `0x21`、続けて `0x12` または `0x14` が来ることがある。

---

## Android実装

1. **イベントパーサ**（実装済み）
   TX notify の `[0x00, 0x55, code]` で `code == 0x14` を `single_tap` として分岐する。  
   未知コードとして捨てない。

2. **ログ／デバッグ UI**（実装済み）
   少なくとも double と同様に画面または logcat に `single_tap` を出す（動作確認用）。

3. **UX**
   - single: FW が録音トグル。アプリは `0x01`/`0x02` でセッション開始/終了。  
     `0x14` で RX `0x01`/`0x00` は送らない（Handy 同時接続時の二重トグル防止）。  
   - double: Kindle / G2 / 割り込み専用。録音トグルしない。

4. **既存 double 処理との共存**  
   - double 専用ロジックを single に流用しない  
   - `0x12` のあとに `0x01`/`0x02` は出ない（0.0.93+）

5. **回帰**  
   - 通常/運転: 1 タップ → `0x14` + 録音トグル + `0x01` or `0x02`  
   - 通常/運転: ダブル → `0x12` のみ（録音不変）  
   - ジェスチャー開始（通常）は従来どおり `0x01`/`0x02`

---

## 参照実装・実機検証（Mac）

- `mac_client/tap_monitor.py` — TX通知を全件ダンプする。`0x12` / `0x14` に加えて
  診断イベント `0xD0`（レジスタスナップショット）/ `0xD1`（生 `TAP_SRC`）/ `0xD2` を
  デコードし、`--write REG:VAL` でOTAなしにIMUレジスタを振れる。
  開始・終了時に音を鳴らすので、1人で叩きながら計測できる。
- `mac_client/image_state.py` — 稼働中のスロット構成を読むだけの確認用。
- `mac_client/xiao_voice_client.py`、`mac_client/gesture_monitor.py` — 既存クライアント。

Androidが受け取れない場合は、まず `tap_monitor.py` でMac直結で受かるかを見る。
Macで受かるならAndroid側、Macでも受からないならファーム側と切り分けられる。

---

## 完了条件（Android）

- [x] `0x14` をパースし、ログで識別できる
- [x] singleは通常／運転とも通知・診断のみと定義されている
- [x] 上記回帰チェックを実機で PASS（2026-08-31、ファーム `0.0.87`）
- [x] 本ドキュメント先頭の「未実装」を外す（ファイル名も改称）

---

## 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-08-30 | ファーム0.0.76でsingle_tap実装・OTA |
| 2026-08-30 | Android `0x14`パース、ログ記録、double回帰修正を実装。ファーム0.0.77をOTA |
| 2026-08-30 | 0.0.76〜0.0.80 の `0x12` 消失原因のひとつを特定（single/double のクールダウン共有）。ファーム0.0.81でクールダウン分離・保留400ms・閾値0.5g/Z軸へ戻し |
| 2026-08-31 | 0.0.82〜0.0.84 でタップ経路をBLEに計装（`0xD0`/`0xD1`/`0xD2`、RX `0x50`/`0x51`）。Mac直結の実測で**真因を特定**: `0.0.76` が `MD1_CFG` にsingle割り込みを追加したため1打目でINT1が上がり、そこで `TAP_SRC` を読むとLIRラッチが解除されてHWのダブルタップ判定が中断されていた |
| 2026-08-31 | ファーム0.0.85〜0.0.87でINT1レベル監視＋350ms遅延読み出しへ変更。single 5/5・double 5/5・誤検出0でPASS。Androidアプリでも受信を確認 |
| 2026-09-01 | 0.0.93: single が全モードで録音トグル、double は非録音。Echo 名 `HarnessNode-Echo`。Handy/Android は `0x01`/`0x02` 追従 |
