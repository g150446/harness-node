# 【Android 未実装】シングルタップ（`0x14`）イベント対応

> **この文書は Android アプリ側の未実装作業メモです。**  
> ファームウェア（`nordic-main`）側は **0.0.76+** で実装済み・OTA 済み。  
> Android クライアントはまだ `single_tap` を解釈・表示・UX に使っていない。

関連（実装済み）:

- ファーム仕様: [`nordic_main_guide.md`](nordic_main_guide.md) の「シングル／ダブルタップ」
- 実装: `nordic-main/src/main.c`（`EVT_SINGLE_TAP` / `process_tap_event`）

---

## 現状

| 層 | 状態 |
|----|------|
| ファーム `0.0.76+` | HW シングル／ダブルタップ検出、BLE 通知 |
| Mac クライアント | `0x14` 未対応（任意のデバッグ表示のみ後追い可） |
| **Android アプリ** | **未実装（本ドキュメントの対象）** |

---

## プロトコル（Android が受けるもの）

### BLE 基本

| 項目 | 値 |
|------|-----|
| デバイス名 | `HarnessNode` |
| Service | `00000001-0000-1000-8000-00805f9b34fb` |
| TX（Notify） | `00000002-0000-1000-8000-00805f9b34fb` |
| RX（Write） | `00000003-0000-1000-8000-00805f9b34fb` |

イベントパケット（TX notify）:

```
[0x00][0x55][code][+ optional payload]
```

### タップ関連コード

| code | 名前 | payload | 意味 |
|------|------|---------|------|
| `0x12` | `double_tap` | なし | リストバンド表側→基板面の垂直ダブルタップ |
| **`0x14`** | **`single_tap`** | **なし** | 同上のシングルタップ（間隔窓を超えた単独衝撃） |
| `0x21` | `sleep_wake` | なし | ライトスリープ復帰（タップでも発生し得る） |
| `0x01` / `0x02` | recording start/stop | なし | 録音開始／停止（ジェスチャー or 運転モード double） |

**注意:** `0x13` は別用途（IMU sample デバッグ）で使われている。`single_tap` は **`0x14` 固定**。

### ファーム側セマンティクス（Android 設計の前提）

| モード | `0x14` single | `0x12` double |
|--------|---------------|---------------|
| 通常 | BLE 通知のみ。録音は変えない。ジェスチャー開始候補を破棄 | 同左 |
| 運転 | **BLE 通知のみ**（録音トグルなし） | primary 接続中に録音開始／停止トグル。併せて `0x01`/`0x02` が出ることがある |

タイミング:

- 物理 1 タップ → HW が最大約 **308 ms** 待ってから single 割り込み
- 約 308 ms 以内の 2 回目 → double のみ（single は出さない想定）
- 認識後クールダウン共有 **700 ms**

ライトスリープ中のタップ: 先に `0x21`、続けて `0x12` または `0x14` が来ることがある。

---

## Android でやるべきこと（チェックリスト）

1. **イベントパーサ**  
   TX notify の `[0x00, 0x55, code]` で `code == 0x14` を `single_tap` として分岐する。  
   未知コードとして捨てない。

2. **ログ／デバッグ UI**  
   少なくとも double と同様に画面または logcat に `single_tap` を出す（動作確認用）。

3. **UX 割り当て（プロダクト決定）**  
   通常モードでは録音を変えない前提。例:  
   - マーカー／ブックマーク  
   - ミュート切替  
   - 画面フラッシュやハプティック  
   運転モードの single は「通知のみ」なので、録音トグルに使わない（トグルは double）。

4. **既存 double 処理との共存**  
   - double 専用ロジックを single に流用しない  
   - 運転モードで `0x12` のあとに `0x01`/`0x02` が続く既存パスを壊さない

5. **回帰**  
   - 通常: 1 タップ → `0x14` のみ（録音開始しない）  
   - 通常: ダブル → `0x12` のみ  
   - 運転: ダブル → 録音トグル + `0x12`  
   - 運転: シングル → `0x14` のみ（録音不変）

---

## 参照実装（Mac・参考）

ファーム接続とイベント分岐の参考（現状は `0x12` まで）:

- `mac_client/xiao_voice_client.py`
- `mac_client/gesture_monitor.py`

Android 実装時は上記に `0x14` を足すのと同じ分岐を、既存のイベント dispatch に追加すればよい。

---

## 完了条件（Android）

- [ ] `0x14` をパースし、ログまたは UI で識別できる
- [ ] プロダクト上の single アクションが通常／運転で定義され実装されている
- [ ] 上記回帰チェックを実機で PASS
- [ ] 本ドキュメント先頭の「未実装」を外し、実装日・アプリ版を追記する

---

## 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-08-30 | ファーム 0.0.76 で single_tap 実装・OTA 済み。Android 未実装として本ドキュメント作成 |
